# vi keybindings in copy mode (scrollback, selection, yank) and at the
# command prompt (<prefix> :). Set explicitly rather than relying on $EDITOR,
# which tmux only consults for status-keys at server start.
set -g mode-keys vi
set -g status-keys vi
