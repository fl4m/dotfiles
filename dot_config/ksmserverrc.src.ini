# Session management is managed by modify_ksmserverrc.tmpl: everything is
# ignored (left to the live system) except loginMode, which is enforced via
# `set`. This source file is intentionally otherwise empty.
