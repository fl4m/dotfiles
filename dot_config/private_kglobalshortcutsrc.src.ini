# Global shortcuts are managed by modify_private_kglobalshortcutsrc.tmpl:
# everything is ignored (left to live/KDE defaults) except the few entries the
# script enforces via `set`. This source file is intentionally otherwise empty.
